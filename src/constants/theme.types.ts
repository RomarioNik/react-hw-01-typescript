interface Colors {
  mainText: string;
  secondText: string;
  bg: string;
  bgComponent: string;
}

interface Shadow {
  shadowComponent: string;
}

export interface GlobalStyleTypes {
  colors: Colors;
  shadow: Shadow;
}
